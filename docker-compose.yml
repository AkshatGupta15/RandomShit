version: '3.8'
services:
  backend:
    build: 
      context: ./backend
      dockerfile: ../deploy/Dockerfile.backend
    ports:
      - "8080:8080"
  frontend:
    build:
      context: ./frontend
      dockerfile: ../deploy/Dockerfile.frontend
    ports:
      - "5173:5173"
